# Core dependencies
gradio>=3.50.2
transformers>=4.34.0
torch>=2.0.0

# RAG Dependencies
langchain>=0.0.267
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

# PDF Processing (options - install at least one)
pymupdf>=1.22.5  # Primary PDF processor (fitz)
pdfplumber>=0.10.0  # Alternative PDF processor

# Image and video processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Fallback dependencies (for when LangChain is not installed)
scikit-learn>=1.3.0

# Utilities
numpy>=1.24.0
